.idea/

# User may potentially want to create a number of build different build
# directories, like: 'build', 'build-release', 'build-old', etc.
build*/

# These files are generated by QtCreator to store various user
# preferences (code style, build configurations) and other data (like the
# heights of tool panels).
*.user
